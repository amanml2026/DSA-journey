#include <stdio.h>
#include <stdlib.h>

// array struct
struct Array{
        int A[10];
        int size;
        int length;
};

int main()
{
    struct Array arr = {{2,4,6,8,10},10,5}; // array
    

    return 0;
}